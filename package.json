{
  "name": "riley-test-widget",
  "version": "1.0.0",
  "description": "Riley AI Voice Agent Test Widget",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild public/app.js --bundle --outfile=public/bundle.js --format=iife --global-name=App"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "retell-sdk": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "retell-client-js-sdk": "^2.0.7"
  },
  "engines": {
    "node": ">=18"
  }
}
